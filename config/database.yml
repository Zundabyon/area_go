# 初期設定はここに
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

# 開発環境はここに記載
development:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>

# 本番環境はここに記載
production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
